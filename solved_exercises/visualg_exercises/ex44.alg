algoritmo "ex44"

var
  x, y: Inteiro
Procedimento Sum(a, b: Inteiro)
inicio
      Escreval("I received the value ",a)
      Escreval("I received the value ",b)
      Escreval("The sum between the two is ",a + b)
FimProcedimento
inicio
      x <- 5
      y <- 3
      Sum(x, y)
fimalgoritmo
